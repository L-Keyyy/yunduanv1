# 加密方式

本文档说明本项目发布前对程序和浏览器扩展进行保护的方式。这里的“加密”主要指前端代码混淆、发布包隔离、授权锁定和敏感信息外置；浏览器扩展属于客户端代码，无法做到绝对不可逆，因此真正的账号、密码、API Key、Cookie 校验必须放在云端服务端完成。

## 1. 保护目标

- 防止直接交付源码目录给客户或第三方。
- 降低扩展 JavaScript 被直接阅读、复制、二次修改的风险。
- 去掉发布包里的本地数据、缓存、日志、`.env` 等敏感文件。
- 强制扩展依赖云端登录态运行，未登录云端或无有效 Cookie 时禁用核心功能。
- 服务端保存账号、店铺 API Key、Cookie 等敏感数据时使用后端加密字段，不在扩展源码中暴露真实密钥。

## 2. 当前项目里的保护目录

| 类型 | 源码目录 | 保护版目录/产物 | 说明 |
| --- | --- | --- | --- |
| Ozon 扩展 | `ozon-weight-extension/` | `ozon-weight-extension-protected/`、`ozon-weight-extension-protected.zip` | 给客户安装或发布时使用保护版，不直接给源码版。 |
| OZON_HD 扩展 | `OZON_HD/extension/` | `OZON_HD/protected/extension/` | 由 `OZON_HD/scripts/protect-extension.js` 生成。 |
| OZON_HD 整体程序 | `OZON_HD/` | `OZON_HD/protected/OZON_HD/` | 由 `OZON_HD/scripts/protect-release.js` 生成。 |

## 3. JavaScript 混淆方式

项目使用 `javascript-obfuscator` 对 `.js` 文件做发布前混淆，依赖在 `OZON_HD/package.json` 中声明：

```bash
npm install
```

核心混淆策略：

- `compact: true`：压缩输出，去除可读格式。
- `controlFlowFlattening: true`：控制流扁平化，降低函数逻辑可读性。
- `deadCodeInjection: true`：插入无效代码，增加逆向分析成本。
- `identifierNamesGenerator: hexadecimal`：变量名、函数名改为十六进制风格。
- `numbersToExpressions: true`：数字常量转表达式。
- `splitStrings: true`：拆分字符串。
- `stringArray: true`：字符串数组化。
- `stringArrayCallsTransform: true`：字符串访问调用转换。
- `stringArrayEncoding: ['rc4']`：扩展保护脚本对字符串数组使用 RC4 编码。
- `stringArrayRotate / Shuffle / IndexShift: true`：打乱字符串数组索引和顺序。
- `selfDefending: true`：加入自防护逻辑，降低格式化后阅读效果。
- `transformObjectKeys: true`：对象 key 转换。
- `target: browser` 或 `target: node`：浏览器扩展使用 browser，Node 程序使用 node。

注意：混淆不是数学意义上的不可逆加密。它只能提高复制和逆向成本，不能把客户端里的固定密钥变成安全密钥。

## 4. 扩展保护版生成

OZON_HD 扩展保护命令：

```bash
cd OZON_HD
npm install
npm run protect:extension
```

该命令会执行：

```bash
node scripts/protect-extension.js
```

处理规则：

- 读取 `OZON_HD/extension/`。
- 输出到 `OZON_HD/protected/extension/`。
- `.js` 文件执行混淆。
- `.html` 文件保留结构，只追加版权声明。
- 图片、manifest 等非 JS 文件原样复制。
- 输出路径被限制在 `OZON_HD/protected/` 下，避免误写到其他目录。

## 5. 整体发布包保护

OZON_HD 整体程序保护命令：

```bash
cd OZON_HD
npm install
npm run protect
```

该命令会执行：

```bash
node scripts/protect-release.js
```

处理规则：

- 扫描 `OZON_HD/` 下的发布文件。
- `.js` 文件执行混淆。
- `.html`、`.ps1`、`.yml`、`.yaml`、`.conf`、`Dockerfile` 等文本代码文件追加版权声明。
- 自动跳过不应进入发布包的目录和文件。

默认排除项：

- `.git/`
- `node_modules/`
- `protected/`
- `cache/`
- `output/`
- `images/`
- `screenshots/`
- `server/cache/`
- `server/data/`
- `server/images/`
- `.env`、`.env.*`
- `*.log`
- `ozon.txt`
- `ozon-product-*.json`

只给源码加版权声明、不生成保护包时使用：

```bash
cd OZON_HD
npm run protect:stamp
```

## 6. 云端授权锁定方式

扩展不应该暴露 `serverUrl`、`ApiKey`、`Authorization`、`localhost` 等可由用户自由填写的配置入口。发布版应固定连接正式云端，并通过云端登录态判断是否允许使用。

推荐锁定逻辑：

- 扩展启动时检查云端域名下的登录 Cookie。
- 没有有效 Cookie 时，核心按钮置灰或直接拒绝执行。
- 有 Cookie 后请求云端 `/api/v1/me` 或等价接口验证登录态。
- 登录态有效才允许采集、跟卖、上货等功能。
- 扩展只携带登录态调用云端，不在本地保存真实管理员密码、Ozon API Key 或服务端密钥。
- 服务端再次校验用户身份、租户权限和接口权限，不能只依赖扩展前端判断。

## 7. 服务端敏感字段保护

服务端保存敏感信息时采用后端加密字段，不把明文长期暴露在前端：

- 店铺 `client_id`、`api_key` 使用后端加密字段保存。
- 前台采集 Cookie、User-Agent 等敏感字段使用后端加密字段保存。
- 加密密钥必须从服务器环境变量读取，例如 `FIELD_ENCRYPTION_KEY`。
- 发布环境禁止使用默认弱密钥、示例密钥或空密钥。
- `.env` 文件不得进入保护包或压缩包。

## 8. 发布前检查清单

发布保护版前必须检查：

- 只交付 `*-protected/` 或 `*-protected.zip`，不交付源码目录。
- 保护版 `.js` 文件已混淆，变量名和字符串不再保持源码可读状态。
- 发布包内没有 `.env`、日志、缓存、Cookie、LocalStorage、测试数据。
- 扩展里没有 `localhost`、测试域名、旧 IP、旧域名。
- 云端地址统一为正式域名，例如 `https://www.omtozon.com`。
- manifest 的 `host_permissions` 只保留正式云端和必要的 Ozon 域名。
- 扩展内没有明文管理员密码、数据库密码、Ozon API Key、JWT Secret、字段加密密钥。
- 登录按钮、SaaS 跳转、接口请求都指向正式云端域名。
- 未登录云端或 Cookie 无效时，扩展核心功能不可用。
- 上架、采集、跟卖等关键接口在服务端也有权限校验。

## 9. 交付说明

推荐交付内容：

- `ozon-weight-extension-protected.zip`
- 或 `ozon-weight-extension-protected/`
- 云端正式访问地址
- 用户账号开通说明

不推荐交付内容：

- `ozon-weight-extension/` 源码目录
- `OZON_HD/` 未保护源码目录
- `.env` 文件
- 数据库备份
- Cookie 文件
- 含真实账号密码的说明文档

## 10. 重要限制

- 浏览器扩展运行在用户本机，代码最终会被浏览器加载，所以不能把客户端混淆当成绝对安全。
- 混淆只能提高阅读和复制成本，不能替代服务端鉴权、租户校验、接口限流和密钥管理。
- 真正的商业授权应该由云端控制：账号、套餐、设备、IP、租户、调用频率都应在服务端校验。
- 如果需要更强保护，可以增加设备绑定、租户授权、服务端签名、接口频率限制、异常行为封禁和版本强制更新。
